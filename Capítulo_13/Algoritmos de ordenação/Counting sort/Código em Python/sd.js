// Exemplo de array
const fruits = ["apple", "banana", "cherry", "date"];

// Usando forEach para iterar com índice e valor
fruits.forEach((fruit, index) => {
    console.log(`Index: ${index}, Fruit: ${fruit}`);
});